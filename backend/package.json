{
  "name": "backend",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx --env-file=.env src/db/seed/index.mts",
    "db:generate": "drizzle-kit generate",
    "db:up": "drizzle-kit push",
    "db:down": "drizzle-kit drop",
    "db:show": "drizzle-kit studio",
    "test": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "better-sqlite3": "^11.1.2",
    "drizzle-orm": "^0.32.1",
    "hono": "^4.5.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.11.17",
    "drizzle-kit": "^0.23.0",
    "tsx": "^4.7.1",
    "vitest": "^2.0.5"
  },
  "volta": {
    "node": "20.16.0"
  }
}